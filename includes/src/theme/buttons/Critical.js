import React from 'react';
import { Button } from 'react-toolbox/lib/button';
import theme from './critical.scss';

const CriticalButton = (props) => <Button {...props} theme={theme} />;
export default CriticalButton;
