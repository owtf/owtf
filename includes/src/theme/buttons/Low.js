import React from 'react';
import { Button } from 'react-toolbox/lib/button';
import theme from './low.scss';

const LowButton = (props) => <Button {...props} theme={theme} />;
export default LowButton;
