import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class RankButtons extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

RankButtons.propTypes = propTypes;
export default RankButtons;
