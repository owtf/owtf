import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class TransactionFooter extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

TransactionFooter.propTypes = propTypes;
export default TransactionFooter;
