import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class SideFilters extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

SideFilters.propTypes = propTypes;
export default SideFilters;
