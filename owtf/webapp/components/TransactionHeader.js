import React, { Component, PropTypes } from 'react';

const propTypes = {
};

class TransactionHeader extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

TransactionHeader.propTypes = propTypes;
export default TransactionHeader;
